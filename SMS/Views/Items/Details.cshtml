@model SMS.Models.Item

@{
    ViewData["Title"] = "Details";
}

<h1>Item Details</h1>

<div>
    <h4>Item</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.item_name)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.item_name)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.item_status)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.item_status)
        </dd>
    </dl>
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model?.item_id" class="btn btn-primary">Edit</a> |
    <a asp-action="Index">Back to List</a>
</div>
